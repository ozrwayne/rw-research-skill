# RW Research Lab Router 案例和反例

> 公开案例使用合成或占位输入，不来自任何个人研究项目。

## 案例 1

- 输入：为 20 篇论文的 DOI 核验选择工具。
- 必须做：确认任务、输入格式、数据敏感性、运行环境、交付物和可接受失败方式。；运行或描述最小测试，记录版本、命令、结果、失败和停止条件。；首选工具、回退和选择理由。
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：rw-paper-extractor。

## 案例 2

- 输入：判断一个文献雷达 repo 对当前综述有没有用。
- 必须做：确认任务、输入格式、数据敏感性、运行环境、交付物和可接受失败方式。；运行或描述最小测试，记录版本、命令、结果、失败和停止条件。；首选工具、回退和选择理由。
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：rw-literature-discovery。

## 案例 3

- 输入：本机没有 research-lab，给我一个可执行的工具路径。
- 必须做：确认任务、输入格式、数据敏感性、运行环境、交付物和可接受失败方式。；运行或描述最小测试，记录版本、命令、结果、失败和停止条件。；首选工具、回退和选择理由。
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：rw-literature-discovery。

## 案例 4

- 输入：为敏感访谈材料选择本地提取流程。
- 必须做：确认任务、输入格式、数据敏感性、运行环境、交付物和可接受失败方式。；运行或描述最小测试，记录版本、命令、结果、失败和停止条件。；首选工具、回退和选择理由。
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：rw-paper-extractor。

## 反例 1

- 输入：代码能编译，所以宣布 API 当前可用。
- 必须做：说明停止条件、缺失信息和可安全继续的部分
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：停止并说明原因。

## 案例 5

- 输入：读取一份工具登记表，判断哪些工具今天可以直接运行。
- 必须做：记录登记版本、许可证、运行环境和最后测试日期；用 freshness 状态区分当前、过期和未知；把登记值与最小运行测试分开。
- 不能做：把登记表当成当前可用性的证明；覆盖与源码或运行结果冲突的版本；把私人路径和凭证写入公开输出。
- 下一步：rw-literature-discovery。

## 反例 2

- 输入：把敏感数据上传到未批准的免费工具试一下。
- 必须做：说明停止条件、缺失信息和可安全继续的部分
- 不能做：依赖未提供的本地工作区内容；生成不存在的论文、数据、DOI、工具结果或期刊要求；越过停止条件：没有运行证据时，不声称工具当前可用。
- 下一步：停止并说明原因。
